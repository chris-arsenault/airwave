# Existing device/SOAP/playback handlers predate the shared CI
# `-W clippy::cognitive_complexity` gate. Keep the lint enabled, but avoid
# blocking deploys until those handlers can be refactored deliberately.
cognitive-complexity-threshold = 120
too-many-arguments-threshold = 6
type-complexity-threshold = 200
