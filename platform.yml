project: airwave
prefix: airwave
stack:
  - rust
  - typescript
  - android
truenas: true
images:
  - backend
  - frontend
android:
  dir: android
  ci_task: assembleDebug
rust_artifacts:
  binaries:
    - { bin: airwave-server, image: backend }
