project: airwave
prefix: airwave
stack:
  - rust
  - typescript
  - terraform
  - android
truenas: true
images:
  - backend
  - frontend
# The backend reads its collector token at start, so it needs an identity to
# read it with (ahara-trust ADR-0002).
truenas_roles_anywhere:
  backend:
    env_prefix: AWS_RA_BACKEND
android:
  dir: android
  ci_task: assembleDebug
fdroid:
  hostname: fdroid.services.ahara.io
  app_id: io.ahara.airwave
  repo_name: Airwave Control
  repo_description: Native Android widget releases for Airwave.
  apk_glob: app/build/outputs/apk/release/*.apk
  metadata_path: fdroid/metadata/io.ahara.airwave.yml
rust_artifacts:
  binaries:
    - { bin: airwave-server, image: backend }
